# 卒業アルバム閲覧サイト 要件定義書

## 概要
GitHub Pages に公開する「卒業アルバム閲覧サイト」を作成する。  
約400名の生徒のプロフィール写真や、学校行事・部活動・先生の写真を閲覧できる Web サイトとする。  
PC とスマートフォンの両方で快適に使えるレスポンシブデザインを採用する。

---

## 開発環境
- **開発端末**：M4 Mac  
- **ホスティング**：GitHub Pages（無料）  
- **構成**：バックエンドを持たない静的サイト（必要に応じて外部データとして Google スプレッドシートを利用）  
- **データ管理**：Google スプレッドシートを使用し、スプレッドシートを編集することでサイトデータを更新できるようにする

---

## 使用可能な無料フレームワーク（いずれかを選択）
以下のいずれかを選んで実装すること。すべて無料で Mac 上で動作し、静的ファイルを GitHub Pages にデプロイ可能。

1. **Astro**  
   - 静的サイト生成に強く、画像の多いサイトに向いている。  
2. **React**  
   - UI 制御や検索・フィルタの実装に適する。コンポーネント単位で拡張しやすい。  
3. **Next.js**  
   - React ベースの高機能フレームワーク。画像最適化やページ分割に優れる。  
4. **Flutter Web**  
   - スマホアプリ風の見た目をブラウザで再現でき、写真ビューアなどを直感的に作れる。

---

## トップページ要件
- サイトの入口として主要カテゴリへ移動できるメニューを配置する。  
- 想定メニュー：
  - 生徒一覧
  - 学校行事（学校祭・体育祭など）
  - 部活動
  - 先生
  - ランダム表示（ガチャ）  
- PC・スマホ両対応のレスポンシブレイアウトとする。  
- デザインはシンプルでナビゲーションしやすいこと（Antigravity に任せてよい）。

---

## パスワード認証要件
- サイトにアクセスした際、最初にパスワード入力画面を表示する。  
- 正しいパスワードを入力するまで、サイトのコンテンツにアクセスできないようにする。  
- 実装方式：
  - クライアントサイド（JavaScript）による認証  
  - パスワードは設定ファイルまたはコード内で管理  
  - 一度認証に成功したら、セッション中は再入力不要（ブラウザのセッションストレージを利用）  
- UI要件：
  - シンプルで分かりやすいデザイン  
  - パスワード入力フィールドとログインボタンのみ  
  - 誤ったパスワードの場合はエラーメッセージを表示  
- セキュリティレベル：
  - 友人同士での閲覧制限を目的とした簡易的な認証  
  - 技術的な知識がある人による突破は防げないが、一般的なアクセス制限としては十分

---

## 生徒一覧ページ要件
- 約400名の生徒をサムネイル画像＋名前で一覧表示する。  
- 実装する機能：
  - 名前検索（部分一致）
  - ソート（名前順・クラス順・タグ順など）
  - フィルタ（クラス、部活、タグで絞り込み）
  - 写真ごとの複数タグ付与
- 各一覧アイテムを選択すると詳細ページへ遷移する。

---

## 学校行事ページ要件
- 学校祭・体育祭などのスナップ写真を一覧表示する。  
- 生徒一覧と同様の検索・ソート・フィルタ・タグ機能を適用する。  
- 写真を選択すると詳細ページへ移動する。

---

## 部活動・先生ページ要件
- 部活動ごと、先生カテゴリごとに写真を一覧表示する。  
- 生徒一覧と同じ検索・フィルタ・タグ機能を利用できる。  
- 一覧から詳細ページへ遷移可能にする。

---

## 詳細ページ要件
- 選択した写真を大きく表示する。  
- **画像ズーム機能**：画像をクリックすると拡大表示（モーダル/ライトボックス形式）  
- 表示するメタ情報（例）：
  - 氏名
  - クラス
  - タグ
  - 撮影時期
  - コメントなど任意情報  
- 関連写真がある場合はサムネイル表示またはリンクを設ける。

---

## ランダム表示機能（ガチャ）
- 「ランダムで1人選ぶ」ボタンを設置する。  
- ボタン押下で生徒一覧の中からランダムに1名を抽選し、その人物の詳細ページを表示する。  
- 必要に応じて簡単な演出（アニメーション）を追加できるが必須ではない。

---

## データ仕様（概要）
- すべてのデータ（生徒、行事、部活、先生）は **Google スプレッドシートで管理する**。  
- スプレッドシートには、名前・クラス・タグ・写真の参照（ファイルパスまたは公開 URL）・説明文などを列で用意する。  
- サイトはスプレッドシートのデータを元に表示を構築する（スプレッドシート API を直接利用するか、定期的に静的データに変換して使用する）。  
- （必要であれば）スプレッドシートの列名サンプルやテンプレートも作成する。

### スプレッドシートに含める主な列（例）
- id
- name
- class
- tags
- photo_url
-撮影時期
- comment

---

## 非機能要件
- レスポンシブデザイン（iPhone / Android / PC）必須。  
- GitHub Pages 上で高速に動作する静的構成とする。  
- 画像は適切に最適化し、遅延読み込み（Lazy Load）を行う。  
- すべての写真に代替テキスト（alt）を設定し、アクセシビリティに配慮する。

---

## 今後の拡張案（任意）
- 写真のスライドショー機能  
- Google スプレッドシート編集用の簡易管理画面（管理者向け UI）  
- ZIP による一括写真アップロードとメタデータ割当ツール  
- AI を用いた自動タグ提案機能

---

## 備考（AntiGravity に渡すための指示）
- トップページのデザインは任せるが、シンプルでナビゲーションしやすいレイアウトにすること。  
- データは Google スプレッドシートで管理する前提とし、スプレッドシートの構造（必要な列）のサンプルを提示すること。  
- 最終成果物は GitHub Pages に置ける静的ファイル群として納品すること。  
- フレームワークは候補から選択可能にし、選定理由と推奨案を提示すること。  
- 実装時に注意すべきポイント（画像最適化、Lazy Load、アクセシビリティ等）をまとめてドキュメント化してほしい。

---

## 受け渡し方法（簡易）
- 最終成果物：GitHub リポジトリ（静的ファイルと README）  
- データ管理：Google スプレッドシート テンプレート（編集用）  
- ドキュメント：要件定義（本ファイル）、スプレッドシート列説明、デプロイ手順（読み物形式）

---
